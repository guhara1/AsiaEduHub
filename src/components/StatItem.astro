---
export interface Props {
  label: string;
  value: string;
}
const { label, value } = Astro.props;
---
<div>
  <dt class="text-[color:var(--color-text-secondary)]">{label}</dt>
  <dd style="font-weight:500">{value}</dd>
</div>
